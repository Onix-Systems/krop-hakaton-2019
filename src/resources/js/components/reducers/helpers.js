export const createAddressLocalityFilter = (equipments) => {
  return [];
};

export const createSearchFilter = (equipments) => {
  return [];
};

export const createDiagnosticSubgroupFilter = (equipments) => {
  return [];
};

export const createDiagnosticTypeFilter = (equipments) => {
  return [];
};
